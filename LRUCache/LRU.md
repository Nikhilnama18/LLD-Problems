Design LRU Cache
* User should define the cache capacity
* User should be able to add key value pair
* Once the capacity is reached, remove the least recently used one.

Entities
* Cache
  * capacity
  * records<key,value>